#include <iostream>
using namespace std;

void count_sort(int arr[], int len);
int main() {
    
}

void count_sort(int arr[], int len) {
    // 1st find the max, min value
    int maxima = max(arr);
    int minima = min(arr);


    //initializing the count arrays
    
}